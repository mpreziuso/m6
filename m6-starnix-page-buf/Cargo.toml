[package]
name = "m6-starnix-page-buf"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Starnix-fork shim: page_buf (M6 no_std)"

[lib]
name = "page_buf"

[dependencies]
# Crate-rename so the forked code's bare `zx::` resolves to the M6 shim.
zx = { package = "m6-zx-shim", path = "../m6-zx-shim" }
